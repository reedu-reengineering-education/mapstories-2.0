version: '3.3'

services:
  postgis:
    image: postgis/postgis:14-3.2
    ports:
      - '5432:5432'
    env_file:
      - .env
    restart: on-failure
    healthcheck:
      test: 'exit 0'
  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080
  minio:
    image: minio/minio
    restart: always
    ports:
      - "9000:9000"
      - "9090:9090"
    volumes:
      - storage-minio:/data
    environment:
      MINIO_ACCESS_KEY: ZThIdNJywmd7di0r
      MINIO_SECRET_KEY: tzYQr2gOrJTTB0TDAtA2tXh3cc0AcBxZ
      MINIO_ROOT_USER: ROOTUSER
      MINIO_ROOT_PASSWORD: CHANGEME123
    command: server --address ":9000" --console-address ":9090" /data
  createbuckets:
    image: minio/mc
    depends_on:
      - minio
    entrypoint: >
      /bin/sh -c "
      /usr/bin/mc alias set myminio http://minio:9000 ZThIdNJywmd7di0r tzYQr2gOrJTTB0TDAtA2tXh3cc0AcBxZ;
      /usr/bin/mc mb myminio/mapstories20;
      /usr/bin/mc policy set public myminio/mapstories20;
      exit 0;
      "

volumes:
  storage-minio:
    external: true